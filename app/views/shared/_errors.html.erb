<% if @user.errors.any? %>
  <div id="error_explanation">
    <% errors = @user.errors %>
    <h2><%= pluralize(errors.count,"error") %> errors prevented the user from being saved:</h2>
    <ul>
      <% errors.full_messages.each do |message|%>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>